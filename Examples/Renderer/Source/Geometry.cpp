#include "Renderer/Geometry.hpp"
#include "Renderer/Scene.hpp"

namespace Engine
{
    // StaticMeshLOD::~StaticMeshLOD()
    // {
    //     GpuSceneData::ptr->createMeshBuffer(m_vbTexcoord);
    //     GpuSceneData::ptr->createMeshBuffer(m_vbNormals);
    //     GpuSceneData::ptr->createMeshBuffer(m_vbPositions);
    //     GpuSceneData::ptr->createMeshBuffer(m_indexBuffer);
    //     GpuSceneData::ptr->createStructuredBuffer(m_drawArgsSB);
    // }


    // StaticMeshLOD::~StaticMeshLOD()
    // {
    //     GpuSceneData::ptr->freeMeshBuffer(m_vbTexcoord);
    //     GpuSceneData::ptr->freeMeshBuffer(m_vbNormals);
    //     GpuSceneData::ptr->freeMeshBuffer(m_vbPositions);
    //     GpuSceneData::ptr->freeMeshBuffer(m_indexBuffer);
    //     GpuSceneData::ptr->freeStructuredBuffer(m_drawArgsSB);
    // }

} // namespace Engine