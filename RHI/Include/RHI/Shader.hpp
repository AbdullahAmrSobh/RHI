#pragma once

namespace RHI
{

    // struct ShaderBindGroupLayoutData
    // {
    //     size_t count;
    //     ShaderBinding*  bindings;
    // };

    // struct ShaderFile
    // {
    //     size_t size; // total size 
    //     Flags<ShaderStage> stages;

    //     size_t codeOffset;
    //     size_t codeSize;
    //     void* code;

    //     size_t reflectionDataOffset;
    //     size_t reflectionDataSize;
    //     ShaderBindGroupLayoutData* shaderBindGroupLayoutList;
        
    // };

}